# Satchel

A simple, audio-reactive WGSL live-coding environment with seamless reusable components.

# Installation

Use Satchel on your browser [here](https://satchel.samranda.com).

Alternatively, use Node to install TypeScript using `npm install typescript --save-dev`.
Then, build using `npx tsc` and navigate to `index.html` in your browser.